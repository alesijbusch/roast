extends ../../layout/layout

block vars
	- var pageTitle = 'Каталог'
	- var pageBodyClass = ''
	- var dir = './'


block premain
	.container
		.page-top
			+breadcrumbs({
				content2: 'Каталог',
			})
			.page-top__header
				h1.page-top__title Каталог

block main
	include ../../components/catalog-list/catalog-list
	include ../../components/subscription/subscription

block pageModal

block componentsCriticalStyles
	+componentsCriticalStyles-includer()
block vendorScripts
	+vendorScripts-includer()
block scripts
	script(src="./js/vendor/nouislider.min.js" defer)

block componentsStyles
	+style-includer()
block componentsScripts
	+componentsScripts-includer()
